// cueism uses plain CSS (no Tailwind). This empty config exists so PostCSS
// stops its config search here instead of walking up to the parent folder's
// Next.js/Tailwind config, which would otherwise break the build.
export default {
	plugins: {},
};
